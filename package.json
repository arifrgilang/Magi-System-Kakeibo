{
  "name": "expense-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for expense tracking with Notion integration",
  "main": "api/telegram.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
